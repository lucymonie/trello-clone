import React from 'react';

const Header = ({ title }) => (
  <header className="blue darken-3 text-white center">
    <span className="title blue-text text-lighten-3">{ title }</span>
  </header>
)


export default Header;
